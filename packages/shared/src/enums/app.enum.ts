export enum Environment {
  DEVELOPMENT = "development",
  TEST = "test",
  DEMO = "demo",
  TESTNET = "testnet",
  STAGING = "staging",
  PRODUCTION = "production",
}

export enum LogLevel {
  DEBUG = "debug",
  INFO = "info",
  WARN = "warn",
  ERROR = "error",
  FATAL = "fatal",
}
